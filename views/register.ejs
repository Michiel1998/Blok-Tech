<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head.ejs') -%>
  </head>
  <body onload="showRegister()">
    <form id="registerForm" action="/register" method="post">
      <h1>Registreer</h1>
      <label><b>Email-adres</b></label>
      <input
        id="email"
        type="text"
        placeholder="Email-adres"
        name="email"
        pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$"
        title="Must be in the following order: characters@characters.domain (characters followed by an @ sign, followed by more characters, and then a ."
        required
      />
      <p id="emailWarning"></p>
      <label><b>Gebruikersnaam</b></label>
      <input
        id="name"
        type="text"
        placeholder="Gebruikersnaam"
        name="name"
        required
      />
      <p id="naamWarning"></p>
      <label><b>Over jou</b></label>
      <input
        id="name"
        type="text"
        placeholder="Schrijf iets over jezelf"
        name="about"
        required
      />
      <p id="naamWarning"></p>
      <label><b>Favoriete drankje</b></label>
      <button class="generateCocktail" type="button" onclick="getRandomCocktail()">Random</button>
      <input
        id="name"
        class="cocktailTextField"
        type="text"
        placeholder="Wat is je favoriete cocktail?"
        name="cocktail"
      />
      <p id="unameWarning"></p>
      <label><b>Wachtwoord</b></label>
      <input
        id="psw"
        type="password"
        placeholder="Wachtwoord"
        name="psw"
        pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
        required
        title="Must contain at least one  number and one uppercase and lowercase letter, and at least 8 or more characters"
      />
      <p id="pswWarning"></p>

      <button class="registerButton" type="submit">Registreer</button>
      <button class="switchForm" type="button" onclick="showLogin()">
        Heb je al een account? Login
      </button>
   
    </form>
  
    
    <form id="loginForm" action="/register" method="post">
      <h1>Login</h1>
      <label><b>Gebruikersnaam</b></label>
      <input
        id="name"
        type="text"
        placeholder="Gebruikersnaam"
        name="name"
        required
      />
      <p id="unameWarning"></p>
      <label><b>Wachtwoord</b></label>
      <input
        id="psw"
        type="password"
        placeholder="Wachtwoord"
        name="psw"
        required
        title="Must contain at least one  number and one uppercase and lowercase letter, and at least 8 or more characters"
      />
      <p id="pswWarning"></p>
      <button class="loginButton" type="submit">Login</button>
      <button class="switchForm" type="button" onclick="showRegister()">Nog geen account? Registreer </button>
    </form>
  </body>

  <script src="/main.js"></script>
</html>
